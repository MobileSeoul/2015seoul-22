<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/UploadMarketInfoBar"
    >

    <LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include
            android:id="@+id/upload_tool_bar"
            layout="@layout/upload_tool_bar"
            />

        <android.support.v4.view.ViewPager
            android:id="@+id/upload_summary_view_pager"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="#39393a"
            android:layout_gravity="center"
            />

        <ProgressBar
            android:id="@+id/upload_summary_progressbar"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="200px"
            android:layout_height="200px"
            android:layout_centerHorizontal="true"
            android:layout_gravity="center_horizontal"/>

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/ColorPrimary"
            android:text="업로드"
            android:textColor="#FFFFFF"
            android:id="@+id/upload_summary_button"
            android:layout_gravity="center_horizontal|bottom" />

    </LinearLayout>

</FrameLayout>